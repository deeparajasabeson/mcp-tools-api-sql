[project]
name = "mymcpmultiagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Deepa Rajasabeson", email = "deeparajasabeson@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=2.1.1",
    "mssql-python>=1.14.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
mymcpmultiagent = "mymcpmultiagent:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
